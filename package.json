{
  "name": "subscription-management-app",
  "version": "1.0.0",
  "description": "Full-stack subscription tracker with smart billing, lifecycle intelligence, and analytics",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "build": "npm install && cd server && npm install && cd ../client && npm install && npm run build",
    "start": "cd server && node src/server.js",
    "seed": "cd server && npm run seed",
    "seed:prod": "cd server && NODE_ENV=production node src/seeds/seedCategories.js"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
